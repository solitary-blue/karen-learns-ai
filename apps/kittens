#!/usr/bin/env bash

# Require bash >= 5.0
min_major=5
min_minor=0

if (( BASH_VERSINFO[0] < min_major || (BASH_VERSINFO[0] == min_major && BASH_VERSINFO[1] < min_minor) )); then
  echo "ERROR: Bash ${min_major}.${min_minor}+ required. Found ${BASH_VERSION}." >&2
  exit 1
fi

set -euo pipefail

# Launch the Kittens lesson viewer in production mode and open it in your browser.

KITTENS_DIR="$(cd "$(dirname "$0")/../.apps/kittens" && pwd)"
KITTENS_URL="http://localhost:3000"

cd "$KITTENS_DIR" || {
  echo "Could not find kittens app at $KITTENS_DIR" >&2
  exit 1
}

if [ ! -d node_modules ]; then
  echo "Installing dependencies (first run)..."
  npm install --legacy-peer-deps
fi

echo "Building Kittens for presentation mode..."
npm run build

(sleep 2 && open "$KITTENS_URL") &

echo "Starting Kittens in production mode..."
exec npm run start
